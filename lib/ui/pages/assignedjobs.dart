import 'package:flutter/material.dart';

class jobasgn extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("No jobs assigned right now"),
      ),
    );
  }
}